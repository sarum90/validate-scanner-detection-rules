{
  "name": "yaml-file-scanner-action",
  "version": "1.0.0",
  "description": "GitHub Action to scan YAML files and post their contents to an HTTP endpoint",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "yaml",
    "scanner",
    "webhook"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/glob": "^0.4.0"
  }
}